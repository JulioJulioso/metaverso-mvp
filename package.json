{
  "name": "metaverso-mvp",
  "version": "0.1.2",
  "private": true,
  "type": "module",
  "description": "MVP metaverso arquitectónico en navegador (Babylon.js + WebXR), AEC-ready",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "stamp": "node scripts/stamp-build.mjs",
    "build:pages": "npm run stamp && vite build --outDir docs --emptyOutDir",
    "preview": "vite preview"
  },
  "dependencies": {
    "@babylonjs/core": "^8.0.0",
    "@babylonjs/loaders": "^8.0.0"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
